# Runtime deps for the deployed Streamlit app (app.py) — this is the file
# Streamlit Community Cloud installs. Kept lean, and the libraries that touch
# the pickled model are pinned to the versions that created models/serving_bundle.joblib
# so it unpickles cleanly on the host.
#
# For the full project (data download, notebooks, bake-off) use requirements-project.txt.

streamlit>=1.35
matplotlib>=3.8

# pinned for pickle compatibility with the serving bundle
numpy==2.5.2
pandas==3.0.5
scikit-learn==1.9.0
xgboost==3.4.1
shap==0.52.0
joblib==1.6.0
