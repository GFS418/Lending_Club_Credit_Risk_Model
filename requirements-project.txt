# Full project stack — needed to reproduce the pipeline end to end
# (data download, notebooks, model bake-off). For just running the deployed
# app, the lean pinned set in requirements.txt is enough.

# Core data stack
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # fast CSV/parquet + efficient dtypes

# Data acquisition
kaggle>=1.6            # Kaggle API client for programmatic download

# Modeling
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.3
shap>=0.45

# App / reporting
streamlit>=1.35
matplotlib>=3.8
seaborn>=0.13

# Notebook environment
jupyterlab>=4.0
ipykernel>=6.29
tabulate>=0.9          # pandas .to_markdown() for the data-dictionary export
